apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-demoapp.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "my-demoapp.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}










